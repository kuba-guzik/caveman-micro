export const authConfig = {
  loginTimeoutMs: 30000,
  refreshTimeoutMs: 15000,
  sessionCacheTimeoutMs: 2500,
  signerTimeoutMs: 5000,
  retry: {
    strategy: 'exp-backoff',
    attempts: 3,
    delaysMs: [250, 1000, 4000],
    openCircuitSeconds: 45,
  },
};

export function shouldOpenCircuit(consecutiveTimeouts, retryBudgetExhausted) {
  return retryBudgetExhausted || consecutiveTimeouts >= authConfig.retry.attempts;
}

export function summarizeAuthFailure({ requestId, userId, shard, reason }) {
  return `${requestId}:${userId}:${shard}:${reason}`;
}

export const authHint1 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint2 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint3 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint4 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint5 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint6 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint7 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint8 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint9 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint10 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint11 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint12 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint13 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint14 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint15 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint16 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint17 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint18 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint19 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint20 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint21 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint22 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint23 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint24 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint25 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint26 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint27 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint28 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint29 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint30 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint31 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint32 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint33 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint34 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint35 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint36 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint37 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint38 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint39 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint40 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint41 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint42 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint43 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint44 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint45 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint46 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint47 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint48 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint49 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint50 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint51 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint52 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint53 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint54 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint55 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint56 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint57 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint58 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint59 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint60 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint61 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint62 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint63 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint64 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint65 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint66 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint67 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint68 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint69 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint70 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint71 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint72 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint73 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint74 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint75 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint76 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint77 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint78 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint79 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint80 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint81 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint82 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint83 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint84 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint85 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint86 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint87 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint88 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint89 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint90 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint91 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint92 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint93 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint94 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint95 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint96 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint97 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint98 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint99 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint100 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint101 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint102 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint103 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint104 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint105 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint106 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint107 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint108 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint109 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint110 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint111 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint112 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint113 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint114 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint115 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint116 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint117 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint118 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint119 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint120 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint121 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint122 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint123 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint124 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint125 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint126 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint127 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint128 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint129 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint130 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint131 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint132 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint133 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint134 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint135 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint136 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint137 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint138 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint139 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint140 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint141 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint142 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint143 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint144 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint145 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint146 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint147 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint148 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint149 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint150 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint151 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint152 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint153 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint154 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint155 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint156 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint157 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint158 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint159 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint160 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint161 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint162 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint163 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint164 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint165 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint166 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint167 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint168 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint169 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint170 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint171 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint172 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint173 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint174 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint175 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint176 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint177 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint178 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint179 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint180 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint181 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint182 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint183 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint184 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint185 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint186 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint187 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint188 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint189 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint190 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint191 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint192 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint193 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint194 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint195 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint196 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint197 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint198 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint199 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint200 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint201 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint202 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint203 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint204 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint205 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint206 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint207 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint208 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint209 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint210 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint211 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint212 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint213 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint214 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint215 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint216 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint217 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint218 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint219 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
export const authHint220 = "Prefer targeted searches for login timeout, retry, session token, and circuit state instead of reading every line."
